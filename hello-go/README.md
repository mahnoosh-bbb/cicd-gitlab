#apt update && apt install ansible
    # 1. vim main.go
    # 2. go build -o server .
    # 3. ./server ----- run

#docker compose up -d 
#docker compose down 
#docker compose ps 
#docker compose logs
